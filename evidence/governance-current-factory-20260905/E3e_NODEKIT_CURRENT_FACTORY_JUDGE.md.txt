# Current NodeKit factory independent judgment

**Re your request:** make repositories ready for users and developer handoff with a planner, worker and independent judge — this review verifies that the fresh factory result belongs to the current governance package.

**Builder. PASS_CURRENT_GOVERNANCE_PACKAGE_FACTORY_WITH_LIMITS.** The new package was actually packed, installed in a launcher and generated consumer, and exercised through the ordinary factory. Current source identity is `cbd062964a7ada1774848f31408f2ffa3fc2953cfa28a096d398cb116810bf64` at commit `0d2223815ef73b80eeed8ce3dc6b96d16b3a2825`. The archive SHA256 is `efdf7edd77ec47a5e4fff5514950e4eaa76068e93c52a442371163363170bdeb` (834,557 bytes).

The governance code changes the distributable package hash even though template and caseflow runtime bytes remain unchanged from e7. Therefore the earlier factory receipt alone was insufficient. This ordinary run supplies a new committed consumer, browser manifest, archive and release-proof receipt bound to the current source; the independent checks reconstruct that chain. [Exact binding checks](E3e_NODEKIT_FACTORY_JUDGE_BINDINGS.json).

- All **421 packaged files** match current committed source and both original/durable installations. The source hash was independently recomputed before and after the browser replay.
- All **1,675 retained files / 78,042,063 bytes** match originals and the durable manifest. All **372 prior factory artifacts** remain intact.
- The generated archive's **77 files** match actual Git commit `ff2aad17fd55ae994af82d6792d3fc29e074df03`. Factory, release proof and browser manifest digests were independently recomputed.
- All **180 PNG/sidecar pairs** match their bytes, dimensions, run, consumer commit and current package identities. The original grid covers 15 explicit states, six viewports and two themes. Three representative original PNGs were visually inspected here; this is not a claim to have visually rejudged all 180.
- A separate actual installed mobile journey passes **36 checks and four captures**: confirm exact Unicode input, reject without altering canonical content, re-propose, approve, download through the real browser link, independently verify artifact/receipt/case/run hashes, reload, and reopen in a fresh browser context. Console, page and network errors are absent. Source and consumer Git/index/status are unchanged. [Raw independent result](E3e-nodekit-factory-independent-browser-02/report.json), [actual downloaded bundle](E3e-nodekit-factory-independent-browser-02/actual-downloaded-proof.json).

One consumer distinction is explicit: it is **not fully clean** after its normal factory checks. The tracked `proof/build-friction.json` changes from 1,014 archived bytes to 2,473 working bytes because the existing recorder appends tests/demo/eval/browser-contract/proof completion events after the factory creates its Git commit. All other 76 archive files equal working bytes. Both versions are preserved and their precise hashes are in the judgment. No runtime discrepancy was found.

The first independent mobile probe used a hidden desktop action locator and failed. The [failed report](E3e-nodekit-factory-independent-browser/report.json) remains intact; the corrected probe used the existing visible mobile controls and passed with no application edits. Earlier helper construction/encoding errors and the initial overbroad archive-equality assertion are disclosed in the structured judgment.

The ordinary factory's 13 base checks pass in **178,511ms**, classified **warm or unknown**. Its existing release result remains **EASE_NOT_CERTIFIED**, with `submissionReady: false`. This is one local no-key factory lane, not a cold-cache benchmark, public deployment, fresh-human test, cross-platform certification or new product grade. The 180 scenario screenshots do not stand in for 180 end-to-end user journeys.

The whole-PR workflow pre-action and H1/canonical authority requirements remain separate and unresolved. This judgment creates no approval event, signature, attestation or blanket exception. Root owns metadata publication and any commit of the generated factory output. Reviewer context was reused after the earlier team thread-limit rejection; this reviewer authored neither the governance repair nor the ordinary factory run. [Structured judgment](E3e_NODEKIT_CURRENT_FACTORY_JUDGE.json).
