# PR39 maintainer review proposal

A maintainer deciding whether to integrate this project recovery must know exactly which generated-app, recovery and governance changes they are accepting. For example, a browser proof can pass while the workflow containing its required browser installation has no qualifying authority record. The complete material range therefore needs a current review and a valid promotion receipt (canonical evolution event).

One unsigned event proposal covers every one of the 21 actual material paths, with exact before/current bytes, raw Git diff and separately hashed technical review evidence.

Read `material-review-manifest.json`, `material-change.diff`, `event-proposal.json`, `evidence-proposal.json`, and `OPERATOR_STEPS.md`. Nothing here is a canonical event or approval. The evidence result is informational; no invariant or human-acceptance score is awarded. Support reports are exact copies of earlier scoped judgments and receipts, not substitute raw execution oracles. Their raw evidence references remain in the existing project/central recovery packets.

The existing policy requires H1 and names one exportable Ed25519 credential. A valid signature proves control of that credential; it does not prove a human was present. The schema's derived `human-reviewed` label must be read with its recorded H1 limitation.

The proposal binds source commit 0d2223815ef73b80eeed8ce3dc6b96d16b3a2825. Later evidence-only publication may retain that material identity. Any changed material file, PR base or public trust policy requires renewed review and proposal bindings before signing.

All prior records and raw failures stay intact. The reviewer did not locate, read or test any private key and did not sign, record a canonical event, edit the repository/index, rerun the factory, or grant release approval.
