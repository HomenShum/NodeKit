# Complete PR39 review awaiting trusted approval

The current generated-app and governance package is implemented, tested and independently reviewed. This packet makes the remaining whole-PR authority decision concrete: all21material paths, exact Git diff, five claim groups and their18original support reports are bound by material-review-manifest.json.

The draft is `evolution/drafts/evt-pr39-portable-factory-and-governance-20260905.json`. Its new informational evidence record has been added through the existing CLI. Existing canonical events, approvals and evidence bytes are unchanged. The proposed event is still agent-proposed; no approval or canonical promotion was created.

Read [the original reviewer instructions](OPERATOR_STEPS.md.txt), [the exact manifest](material-review-manifest.json), [raw diff](material-change.diff) and [event proposal](event-proposal.json). Copying and informational registration, steps1and3, are now completed; the trusted-credential decision remains. Recheck current PRbase/material bytes before signing. The current public policy requires its existing H1 credential and does not accept the narrower deferred receipt for the earlier workflow change. H1 attests credential control, not human presence or user acceptance.

An operator who accepts the complete current diff supplies their already trusted key locally to the existing `evolution approve` command, then uses the unexpired approval in `evolution record`, as the exact instructions describe. No credential path was discovered, key read, replacement key created or policy weakened by this preparation. The canonical gate remains blocked until that actual operation succeeds. Do not post the private key into a chat or commit it.

The current factory and its independent judgment are in ../governance-current-factory-20260905. Shared draftPR39, releaseReady=false, EASE_NOT_CERTIFIED, human/device validation and full grades remain separate. Historical Markdown uses .md.txt to preserve original bytes and instructions without making them current automated tasks.
