# Handoff — 2026-07-29

Written for the engineer picking this up cold. It states what is true, what is blocked, and what
is not proven. Where a number appears, it was measured, not recalled.

## Start here, in this order

1. `npm ci` — **the repository ships with no `node_modules`**, and almost every confusing failure
   in the last session traced to that. `yaml` is declared at `^2.9.0` and was simply absent, which
   made `ci-workflows` unable to load and `nodekit tour` unable to start.
2. `node scripts/test-files.mjs fast` — the real suite entry point. `node --test test/` does **not**
   work on Node 22 here; it resolves `test` as a module. Use `node --test "test/*.test.mjs"` for a
   subset.
3. Read `docs/VACUOUS_PASS.md` before trusting any green result in this repository, including your
   own.

## Repository state

| repo | branch | pushed | note |
|---|---|---|---|
| node-platform | `feat/close-the-loop` | yes | PR #22 open, blocked — see below |
| nodeslide | `claude/walkthrough-roster` | yes | `main` carries the ported work |
| parity-studio | `feat/phase4-resurface-parity` | yes | |

Every branch across all three was verified reachable from a remote with
`node scripts/unpushed-work-gate.mjs --repo <path>`. Six branches were on **no remote** when
cleanup began and were pushed before any worktree was removed. Run that gate before deleting
anything; `git status` will not tell you.

## The one blocker

**PR #22 cannot merge.** CI `verify` fails:

    EVOLUTION MATERIALITY BLOCKED: 18 material files, 0 recorded events

That is the gate working. `main` is unprotected, so `gh pr merge` would succeed — do not. The gate
exists because eleven of twelve maturity checks were once hand-edited to `true` by the party being
graded, and an agent recording its own material-change approval is the one thing the authority
model forbids.

**The sanctioned path to unblock it is itself broken, and this is probably the highest-value fix
available.** `scripts/propose-evolution.mjs` is the only lane an agent may use to propose a
material change. It cannot produce a valid record: `nodekit.evolution-event.v1` requires
`evidenceIds` to hold at least one `nodekit.evolution-evidence/v1` reference, and the script
exposes no flag to supply one (`--category --challenge --id --limitation --observed --resolution
--supersedes --thread --track`). Its own header says the schema defect "is FIXED upstream" — the
CLI never gained the argument. So the authority model currently has a legal lane that does not
work, and the only way through is a human writing the record by hand.

Do **not** hand-write the JSON to route around it. That script's own comment records what happened
last time: records with 7, 9 and 8 schema errors that "looked entirely official."

## What shipped, and what it does not prove

Four Builder Journey stage schemas, each making one specific dishonesty structurally
unrepresentable — a decision neither honoured nor disclosed fails validation; a claim implying user
data without an authority-issued binding cannot be marked bound; a deploy cannot claim a live
signal without a rendered-DOM observation; "no instances observed" requires a non-zero sample
count.

**They enforce shape, not behaviour.** No producer exists for any of the four. Nothing yet writes a
conforming artifact during a real build, and the chain passes against hand-authored fixtures.

Five gates, each probed in all three directions (pass / fail / not-run):

    node src/lib/journey-chain-gate.mjs test/fixtures/builder-journey   # 5/5 stages, 6/6 digests
    node scripts/reference-corpus-gate.mjs atlas/references
    node scripts/motion-token-drift-gate.mjs <repoA> <repoB> [...]
    node scripts/unpushed-work-gate.mjs --repo <path>
    node scripts/ui-gates/trust-surface-selftest.mjs                    # needs playwright as a peer

## Suite status, stated honestly

Last full run: **536 tests, 532 pass, 4 fail.**

Three of the four **do not reproduce in isolation** — `behavior-index`, `evolution-ledger` and
`knowledge-runtime` each exit 0 on a quiet tree. They failed only inside a full lane contending
with concurrent worktree operations. This repository has produced that pattern three times in one
day. **A suite that reports differently under load is reporting on the machine, not the code** —
re-run a failure in isolation before believing it.

The fourth was real: `repo-map.json` drift, because files landed after the last regeneration.
`npm run repo:map` fixes it. It is derived, so on a merge conflict regenerate rather than resolve.

## Traps that cost real time here

- **`cmd | tail; echo $?` gives you `tail`'s exit code.** This was hit three times in one session,
  including by the person documenting it. Capture the exit on its own line.
- **A background wrapper's exit code is not the wrapped command's.** Twice a run was reported as
  exit 0 while its own captured `EXIT=` line said 1.
- **`git status` reports a CRLF file as clean** when `.gitattributes` normalizes it. `src/cli.mjs`
  carried 1,931 CRLF pairs while the index was pure LF. Use `git ls-files --eol`; look for
  `w/mixed`. npm's `bin-links` rewrites the first shebang of a declared bin from CRLF to LF at
  install time, which broke the packed-vs-installed manifest check by exactly one byte at offset 19.
- **`npm pack` runs `prepare`**, so packing is not a pure read.

## Not proven, and worth knowing before you make claims

- `@homenshum/nodekit` is **unpublished** — `npm view` returns 404. Nothing here is reachable by
  the coding agent it is written for.
- Trial 2 is **unscheduled**. It is the stated exit condition, and behind it is a kill criterion
  written before the result so it could not be renegotiated after. A kill criterion with no
  scheduled test becomes a deferral.
- No beneficiary exists. Everything downstream of trial 2 is blocked on a person.
- The reference corpus holds one collection, four records, tier **M2** (live shipped-product
  observation). No audience-task evidence was collected, so no rule may be promoted to
  audience-proven.
- No live-deployment verification of any ported NodeSlide function.
