## Before boundary and custody

Original three-boundary screenshots are in before-export-boundary/390 and /1440 under the protected operator root; completed state at 390x960 and1440x960, anonymous Chromium light, exact neutral outcome, no simulated render. First two-boundary capture remains retained in before-boundary. Source bindings before and after each capture are exact. Five template Git blobs at53348d6 and4980e9f are identical, recorded separately; current screenshots are nevertheless freshly generated from installed4980 tarball.

Change A: #error only, actual empty34px red strip. Change B: #artifact only, accepted outcome and canonical attributes. Change C: existing #review receipt/export subregion only; bounding parent is shown because currently hidden controls have no pixels. This allows the existing download/share controls to appear for actual completed receipts. Review mode, proposal decisions, focus affordances, labels and activity remain protected. Global header, type system, step navigation, state banner, case navigation, approval policy and runtime semantics remain protected.

The original E5b and NodeProof consumers are immutable (1834 and3988 observed files). Base2180trackedfiles, refs and index are hash-recorded;99,517,763byte bundle verified. Ignored credentials/runtime outside tracked custody remain excluded, not a full-custody claim. Before launcher and consumer are retained separately; new output uses a fresh candidate path.

