**VERIFIED_WITH_CAVEATS** — Builder, independent review of `NODEKIT-CONSUMER-INTERCHANGE-01`.

**Re your request:** make each repository usable for developer/user handoff, with planner–worker–judge execution and actual visual/interaction proof. The frozen NodeKit patch preserves the exact approved outcome across DOM, download and receipt; rejected or stale proposals preserve canonical state, and real error/retry remains visible while hidden error feedback occupies no pixels.

A developer must be able to hand another person the same result they approved. For the exact input containing leading/trailing spaces, `“茶”`, `<receipt>` and `€42.50`, this judge confirmed that the rendered canonical record, downloaded JSON and receipt describe one result. Missing identity fields and recomputed-but-substituted values were rejected (canonical interchange). A completed result is handoff-ready only when its identity, content and receipt can be independently compared.

| Claim | Independently observed |
|---|---|
| Frozen source | Base `4980e9f534ebd9ecc499767a687002a2a68b6667`, exact staged tree `f15e2a2b2d35110a9dff8c5a9932e2c267ae5ae1`, staged path/blob digest `82efa2fac9b78af84d8cda2319afae37092008ca2e5326f307cbecf57049dd57`. All 506 paths match working bytes and Git blobs: ten authored paths plus 496 portable proof files. |
| Package custody | Exact tarball `5964b7b265d3e9f79f49e825abd9682caf3440cc5125ceec1331569b9599b83b`; all 421 packed files match source and both retained installations. Refs, 2,170 protected source files, original consumers with 1,834 and 3,988 files, and the recovery bundle match custody. |
| Repository / consumer | 855/855 repository tests and 10/10 consumer tests passed. Normal `npm ci`, compile, check, demo, eval, HTTP browser contract, audit and local proof passed in judge copies. |
| Full browser lane | 180 captures and all 18 assertions passed. Certification correctly remained **false**, verdict **BROWSER_JOURNEY_PASS_IDENTITY_UNBOUND**, exit **1**. No source commit was invented. |
| Exact user journey | At 390×960 and 1440×960: validation, transport failure/retry, rejection, duplicate approval, actual download, independent hashes, reload, fresh context and stale containment/recovery passed. Duplicate approval sent one request. |
| Causal knockouts | Fourteen additional checks across both widths: restore old error CSS; remove each of four DOM identity fields; trim outcome and recompute its hash; change receipt version and recompute its hash. Each invalid boundary failed and restored state passed. |
| Viewport download | Two additional real completed cases exposed a fully visible download control within the current viewport; error height 0 and horizontal overflow 0. Both downloads succeeded. |
| Bounded stability | 31.31 seconds and 20 mobile reloads preserved the canonical identity and 19-event count. Same running process only. |

The reviewer context was reused after a fresh reviewer could not be started because of the tool thread limit. This reviewer did not author NodeKit implementation changes. It independently read the ten-file diff, examined the real prior boundaries, checked raw bindings, replayed the commands and inspected 14 screenshots. The three metadata changes preserve the existing source references; browser checks were extended and the source/tarball certification gate was not relaxed.

**Boundary proof.** Genuine prior 3px boxes identify the error strip, canonical artifact and existing receipt/export subregion. Matched neutral before/after captures at both widths show the intended changes. The header, hero, navigation and review/activity implementations remain unchanged. Their document positions move as the empty strip disappears and the artifact grows; no absolute-position equality is claimed. One preserved full-page stale-recovery image composites sticky/fixed elements at a nonzero scroll position. It is retained as raw output and excluded from layout certification; the two added viewport images show the actual header at y=0 and the successful download boundary.

**Cause, fix and knockout.** The original flex declaration overrode `hidden`; scoping it to `.error:not([hidden])` removes the empty strip while preserving failure display. Reintroducing old CSS made the recovered error region 79.375px high at both widths; removing it restored zero. Input was lost through trimming and a canned producer; the current scalar and proposal now preserve it without changing Caseflow's normalized case label. Canonical DOM fields and actual completed download controls use existing runtime/receipt owners. All corrupted identity/content/receipt knockouts failed, and the unaltered proof passed after reopening.

The first isolated repository run had 12 failures because the judge copied files without Git metadata. Git-backed evidence queries correctly refused. Its raw failure remains preserved. Restoring independent metadata from the verified base bundle changed no source or tests; the same suite then passed 855/855, and all 2,676 candidate file bytes still matched. This is a corrected judge setup failure, not a hidden product failure.

**Residual limits.** Local proof still has `releaseReady:false`. Browser certification remains false until a reviewed clean commit is bound; the unchanged full factory must then run with `NODEKIT_KEEP_ACCEPTANCE=1`, retaining its launcher, consumer and release proof. Existing ledger/authority, human usability, production, remote CI and host activation holds remain open. The server is a single in-memory demonstration; no restart persistence, concurrency scalability or provider outage was proved. No full visual, responsive, interaction, accessibility, performance or overall score is assigned.

Authored `git diff --check` passes. All-staged `--check` reproduces exit 2 for 27 raw HTML whitespace findings and one trailing blank line in the preserved before-boundary Markdown. Evidence bytes were not sanitized. No candidate source/index/ref change, commit, push, deployment, provider request or host activation was performed by this review.

The exact frozen tree is suitable for the parent's reviewed commit and subsequent unchanged factory replay. Primary evidence: [structured verdict](C:/Users/hshum/.codex/worktrees/5dba/nodebench-ai/docs/plans/portfolio-recovery-20260904/E3c_NODEKIT_CONSUMER_CONTRACT_JUDGE.json), [bindings](D:/ProjectRecovery/nodekit-consumer-contract-20260905/judge/bindings.json), [independent journey](D:/ProjectRecovery/nodekit-consumer-contract-20260905/judge/independent-browser/report.json), [actual viewport evidence](D:/ProjectRecovery/nodekit-consumer-contract-20260905/judge/viewport-boundary/report.json), [browser certification refusal](D:/ProjectRecovery/nodekit-consumer-contract-20260905/judge/consumer/proof/browser-certification.json), [repository rerun log](C:/Users/hshum/.codex/worktrees/5dba/nodebench-ai/docs/plans/portfolio-recovery-20260904/E3c-nodekit-judge-repository-git-context.log). The JSON binds all 506 candidate paths and 67 bounded judge artifacts, including every viewed image; it does not add a redundant bulk hash pass over supplemental raw HTML.
