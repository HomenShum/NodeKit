node_modules/
coverage/
.nodekit/
npm-debug.log*
changes/nodekit-proof-campaign-2026-07-20/video/output/
proof/ease/agents/
/tsconfig.component.tsbuildinfo
/.tmp-component-pack.json
/.tmp/
/.understand-anything/*
!/.understand-anything/.understandignore

# Windows reserved device name. A `2>NUL` redirect run under a POSIX shell on
# Windows creates a real zero-byte file here instead of discarding output.
/NUL

# Downloaded proof fixtures. Five trees, ~22k files each. Untracked and
# unignored, they were swept into a commit by `git add proof/` and killed the
# push with an HTTP 500 pack failure.
proof/ease/downloaded*/
