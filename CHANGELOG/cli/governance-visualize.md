# Changelog — `nodekit governance visualize`

> **Surface**: Static, inspectable governance graph generated by the NodeKit CLI.
>
> **Append rule**: New entries go at the top. Never delete prior entries.

## 2026-07-30 — Visualize the governance decision path

Add a deterministic PR #32 scenario that writes an interactive HTML graph and an adjacent JSON
receipt bundle. The summary-first canvas, persistent inspector, responsive layout, and optional
dark mode are based on atomic Mobbin reference observations rather than copied pixels.

**Commit**: `this commit`. **Author**: Codex.
**Touches**: `CHANGELOG/library/governance.md`, `CHANGELOG/schemas/governance-contracts.md`
